import styled from "styled-components";

export const MainSearchInnerContainer = styled.div`
  position: relative;
  display: block;
  top: 35%;
  transform: translate(0, -50%);
  padding-bottom: 30px;
  z-index: 2;
`;