import { useCustomSubscription } from "app/common/hooks/useCustomSubscription"

export const getSubscription = () => {
  //const [data, loading, error] = useCustomSubscription(QUERY_SUBSCRIPTION)  
}
